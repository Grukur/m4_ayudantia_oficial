import Vocales from "./Vocales.js";

export default class Tipos extends Vocales{
    constructor(nombre, rango, comentario, img, sonido){
        super(nombre, rango, comentario, img, sonido);
    }
}